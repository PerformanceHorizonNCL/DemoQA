class FindingOutlier
  def findOutlier(array)
#select all even numbers
    even = array.select {|num| num.even?}
    odd = array.select {|num| num.odd?}
    if even.count> odd.count
      return odd
    else
      return even
    end
  end
end

